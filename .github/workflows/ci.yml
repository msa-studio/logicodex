# CI Pipeline for Logicodex Language Engine
#
# Architecture:
#   ACTIVE:   v1.21-alpha (stable pipeline — default for all compilation)
#   DORMANT:  v1.30.0-alpha (experimental — opt-in via --pipeline v1.30)
#
# The CI enforces this architecture by:
#   1. Default check/compile → v1.21 pipeline (active)
#   2. v130-check step → verifies dormant subsystem compiles but does not activate
#   3. Example checks → all use default v1.21 semantic validation
#   4. Safety nets → v1.21 codegen panics if v1.30 AST nodes leak (emit_v130_ast_in_v121)
#
# Freestanding-only modules (os/startup, os/allocator, os/uart) are gated with
# #[cfg(target_os = "none")] and excluded from hosted CI builds.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  LLVM_SYS_150_PREFIX: /usr/lib/llvm-15
  RUSTFLAGS: "-L/usr/lib/llvm-15/lib"
  RAYLIB_NO_LINK: "1"

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install LLVM 15
        run: |
          sudo apt-get update
          sudo apt-get install -y llvm-15-dev libclang-15-dev libpolly-15-dev pkg-config

      - name: Install Rust
        uses: dtolnay/rust-toolchain@1.75
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Check formatting
        run: cargo fmt --all -- --check
        continue-on-error: true

      - name: Check build
        run: cargo check 2>&1 | tee build-check.log

      - name: Show build errors
        if: failure()
        run: |
          echo "=== BUILD CHECK ERRORS ==="
          cat build-check.log | grep -E "^error" | head -20
          echo "=== FULL ERROR CONTEXT ==="
          tail -80 build-check.log

      - name: Upload build check log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-check-log
          path: build-check.log

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install LLVM 15
        run: |
          sudo apt-get update
          sudo apt-get install -y llvm-15-dev libclang-15-dev libpolly-15-dev pkg-config

      - uses: dtolnay/rust-toolchain@1.75

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build
        run: cargo build --release --message-format=short 2>&1 | tee build-release.log
        env:
          RUST_BACKTRACE: 1

      - name: Show build errors
        if: failure()
        run: |
          echo "=== BUILD ERRORS ==="
          cat build-release.log | grep -E "^error" | head -20
          echo "=== FULL ERROR CONTEXT ==="
          tail -80 build-release.log

      - name: Upload build release log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-release-log
          path: build-release.log

      - name: Run unit tests
        run: cargo test 2>&1 | tee test-output.log

      - name: Show test failures
        if: failure()
        run: |
          echo "=== TEST FAILURES ==="
          grep -E "^test .* FAILED|^failures:|^thread '.*' panicked" test-output.log | head -30
          echo "=== FULL TEST OUTPUT (last 100 lines) ==="
          tail -100 test-output.log

      - name: Run Tier A validators (core)
        run: |
          for v in scripts/validators/tier_a_core/*.py; do
            echo "=== $(basename "$v") ==="
            python3 "$v" || exit 1
          done

      - name: Run Tier B validators (feature)
        run: |
          for v in scripts/validators/tier_b_feature/*.py; do
            echo "=== $(basename "$v") ==="
            python3 "$v" || exit 1
          done

      # v1.21 ACTIVE: Only examples/*.ldx (root level) — v1.21 stable
      # Dormant v1.30 examples live in examples/dormant/v1_30/ and are
      # NOT checked here — they have their own dormant check step below.
      - name: Check v1.21 examples (ACTIVE pipeline)
        run: |
          set -x
          failed=0
          for f in examples/*.ldx; do
            if [ -f "$f" ]; then
              echo "=== Checking $f (v1.21) ==="
              if ! cargo run -- check "$f" 2>&1 | tee -a example-check.log; then
                echo "FAILED: $f"
                failed=1
              fi
            fi
          done
          echo "=== FULL EXAMPLE CHECK LOG ==="
          cat example-check.log
          if [ $failed -eq 1 ]; then
            echo "=== One or more v1.21 examples failed ==="
            exit 1
          fi

      - name: Upload example check log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: example-check-log
          path: example-check.log

      # v1.30 DORMANT: Subsystem self-check + dormant examples
      # Uses --pipeline v1.30 to activate dormant parser/codegen
      - name: Check dormant v1.30.0-alpha subsystem
        run: |
          echo "=== v1.30 dormant subsystem self-check ==="
          cargo run --quiet -- v130-check examples/hello.ldx || exit 1
          echo "=== v1.30 dormant examples ==="
          for f in examples/dormant/v1_30/*.ldx; do
            if [ -f "$f" ]; then
              echo "=== Checking $f (v1.30 DORMANT) ==="
              cargo run --quiet --pipeline v1.30 -- check "$f" || exit 1
            fi
          done
          echo "=== v1.30 dormant OK ==="
