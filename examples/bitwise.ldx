// Logicodex v1.21 — Bitwise operations (expert/canonical syntax)
{
    let sensor: U32 = 42;
    let mask: U32 = 15;
    let low_bits: U32 = sensor & mask;
    let shifted: U32 = sensor >> 1;
    let routed: U32 = (low_bits | shifted) << 1;
    print routed;
}
